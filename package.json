{
  "name": "mailsignaturegen",
  "version": "2.0.0",
  "description": "Gerador de assinaturas NerdResolve com selecao de segmento",
  "main": "projeto/server.js",
  "scripts": {
    "init": "npm install && python -m pip install -r requirements.txt",
    "start": "node projeto/server.js"
  },
  "dependencies": {
    "express": "^4.22.1",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
